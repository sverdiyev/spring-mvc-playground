open questions:

1. how to make liquibase.properties take db url from application.yml
2. how to make configure profiles without string concatenation like application-${spring.profiles.active} ....
3. remake handler into broker and make it into a controller